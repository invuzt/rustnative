name: Build Rust Android APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Setup Rust
      uses: dtolnay/rust-toolchain@stable
      with:
        targets: aarch64-linux-android

    - name: Rust Cache
      uses: Swatinem/rust-cache@v2

    - name: Install cargo-apk & UPX
      run: |
        cargo install cargo-apk
        sudo apt-get install upx-ucl -y

    - name: Setup Android SDK
      uses: android-actions/setup-android@v3

    - name: Install NDK & Platform 30
      run: |
        ${ANDROID_HOME}/cmdline-tools/latest/bin/sdkmanager --install "platforms;android-30" "ndk;26.1.10909125"
        echo "ANDROID_NDK_HOME=${ANDROID_HOME}/ndk/26.1.10909125" >> $GITHUB_ENV

    - name: Build APK (Release Mode)
      run: cargo apk build --release

    - name: Compress Binary with UPX
      run: |
        # Cari file .so hasil build dan kompres sebelum di-upload
        find target/release/apk -name "*.so" -exec upx --best {} +

    - name: Generate Dummy Keystore
      run: |
        keytool -genkey -v -keystore debug.keystore -alias androiddebugkey -storepass android -keypass android -keyalg RSA -keysize 2048 -validity 10000 -dname "CN=Android Debug,O=Android,C=US"

    - name: Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: odfiz-app
        path: |
          target/release/apk/*.apk
          !target/release/apk/*-unaligned.apk
